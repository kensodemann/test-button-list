import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'test-button-list',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
